# @package _global_

defaults:
  - override /model: mlp
  - override /logger: wandb

total_genes: 2061

data:
  data_name: "norman"

logger:
  wandb:
    tags: ${tags}
    group: "mlp_norman_${data.split}"
