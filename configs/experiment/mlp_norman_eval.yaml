# @package _global_

# run with `python src/eval.py experiment=mlp_norman_eval`

defaults:
  - override /model: mlp
  - override /logger: wandb

total_genes: 2061

data:
  data_name: "norman"
  eval_type: "ARID1A_de"

ckpt_path: "${paths.log_dir}train/runs/earnest-frost-107/checkpoints/best_model_at_epoch_019.ckpt"

logger:
  wandb:
    tags: ["mlp", "norman", "split_${data.split}", "replicate_${data.replicate}"]
    group: "mlp_norman_${data.split}"
